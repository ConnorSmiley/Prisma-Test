import React from "react";
import styled from "styled-components";
import tw from "twin.macro"



const EmailPageContainer = styled.div`
    ${tw`
    
    `}
`

const EmailPageStyle = styled.div`
    ${tw`
    
    `}
`

export interface IEmailPageProps {

}

const EmailPage: React.FC<IEmailPageProps> = () => {

    return(
       <>
        <EmailPageContainer>
            <EmailPageStyle>

            </EmailPageStyle>
        </EmailPageContainer>
       </>
    )
}

export default EmailPage
